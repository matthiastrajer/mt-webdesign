---
import { getEntry } from 'astro:content';

const settingsEntry = await getEntry('settings', 'site');
const s = settingsEntry!.data;

// wa.me erwartet die Nummer in reiner Ziffernform mit Ländervorwahl,
// ohne "+", Leerzeichen oder Bindestriche. Das Anzeige-Format in
// site.json darf dadurch beliebig formatiert sein.
const waNumber = (s.phone ?? '').replace(/\D/g, '');
const waHref = waNumber
  ? `https://wa.me/${waNumber}${s.whatsappText ? `?text=${encodeURIComponent(s.whatsappText)}` : ''}`
  : null;
---

{waHref && (
  <a
    href={waHref}
    class="wa-fab"
    target="_blank"
    rel="noopener noreferrer"
    aria-label="Per WhatsApp Kontakt aufnehmen (öffnet WhatsApp in einem neuen Tab)"
  >
    <svg
      class="wa-fab__icon"
      width="26"
      height="26"
      viewBox="0 0 24 24"
      fill="currentColor"
      aria-hidden="true"
    >
      <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z" />
    </svg>
    <span class="wa-fab__label">WhatsApp</span>
  </a>
)}

<style>
  /* Schwebender Kontakt-Button unten rechts.
     z-index bewusst unter Nav-Overlay (39) und Consent-Banner (90) —
     beide sind situativ wichtiger und dürfen den Button überdecken. */
  .wa-fab {
    position: fixed;
    right: clamp(1rem, 3vw, 1.75rem);
    bottom: clamp(1rem, 3vw, 1.75rem);
    z-index: 35;

    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    padding: 0.85rem 1.15rem;

    /* WhatsApp-Markengrün — bewusst außerhalb der Design-Tokens, weil der
       Wiedererkennungswert hier wichtiger ist als Farbschema-Konsistenz.
       #075E54 (WhatsApp Dark) statt #25D366: gleicher Marken-Kontext, aber
       weißer Text/Icon erreicht darauf sicher AA-Kontrast. */
    background: #075e54;
    color: #fff;
    border-radius: 999px;
    box-shadow: 0 4px 16px rgba(7, 94, 84, 0.28);

    font-family: var(--font-display);
    font-size: 15px;
    font-weight: 500;
    letter-spacing: -0.005em;
    line-height: 1;

    transition: transform var(--dur) var(--ease-out),
      box-shadow var(--dur) var(--ease-out),
      background var(--dur) var(--ease-out);
  }

  .wa-fab:hover,
  .wa-fab:focus-visible {
    background: #0b7d6f;
    transform: translateY(-2px);
    box-shadow: 0 6px 22px rgba(7, 94, 84, 0.36);
  }

  .wa-fab__icon {
    flex-shrink: 0;
  }

  /* Auf schmalen Screens nur das Icon — spart Platz und verdeckt weniger
     Inhalt, bleibt aber per aria-label für Screenreader beschriftet. */
  @media (max-width: 640px) {
    .wa-fab {
      padding: 0;
      width: 54px;
      height: 54px;
      justify-content: center;
      gap: 0;
    }

    .wa-fab__label {
      display: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .wa-fab {
      transition: none;
    }
    .wa-fab:hover,
    .wa-fab:focus-visible {
      transform: none;
    }
  }
</style>
